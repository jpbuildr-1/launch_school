# Without running the following code, what do you think it will do?
def foo(bar, qux):
	print(bar)
	print(qux)

foo(42, 3.141592, 2.718) # This will return an error because you are passing an extra argument when you should not be.