# Given a list of numbers [1, 2, 3, 4, 5], mutate the list by removing the number at index `2`, so that
# the list become [1, 2, 4, 5].

numbers = [1, 2, 3, 4, 5]
# numbers.pop(2)
del numbers[2]
numbers
print(numbers)